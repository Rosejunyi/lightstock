
# Python依赖包
# 用于GitHub Actions和本地开发

# 核心数据处理
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0          # ← 重要!用于读写parquet文件

# 数据源
baostock>=0.8.9
akshare>=1.12.0

# 进度条
tqdm>=4.65.0             # ← 重要!用于显示进度

# 环境配置
python-dotenv>=1.0.0

# 数据库(可选)
supabase>=2.0.0

# 日期时间
python-dateutil>=2.8.0
